@model IEnumerable<StraussDA.BlogLib.Post>

<div class="collapse fade" id="submenu" role="dialog " aria-labelledby="exampleModalCenterTitle " aria-hidden="true ">
    <div class="navbar-expand-lg bg-light ">
        <div class="row no-gutters ">

            <div class="col-4 ">
                <h4 class="pl-4 submenuhead ">Recent Articles</h4>
                 <ul class="col ">
                        <ul class="list-unstyled">
                            @foreach (var item in Model)
                            {
                                int maxLeft = 5;
                                if (maxLeft > 0)
                                {
                                    <li> @Html.ActionLink(item.PostName, "Details", new { id = item.Id }, new { @class = "btn btn-info" })</li>
                                    @*<a class="nav-link text-dark" asp-area="" asp-controller="Post" asp-action="Index"> @Html.DisplayFor(modelItem => item.PostName) @*/Modified: @Html.DisplayFor(modelItem => item.ModifyDate)*@

                                    maxLeft--;
                                }
                                else
                                {
                                    return;
                                }
                            }
                            <div class="col ">
                                <ul class="list-unstyled "></ul>
                            </div>

                            <div class="col ">
                                <ul class="list-unstyled "></ul>
                            </div>
                </div>
           <div class="col ">
                    <h4 class="submenuhead ">Web Development</h4>
                    <ul class="list-unstyled ">
                        @foreach (var item in Model.Where(c => c.PostCategory == "Web Development" ))
                        {
                            int maxLeft = 5;
                            if (maxLeft > 0)
                            {
                                <li> @Html.ActionLink(item.PostName, "Details", new { id = item.Id }, new { @class = "btn btn-info" })</li>
                                @*<a class="nav-link text-dark" asp-area="" asp-controller="Post" asp-action="Index"> @Html.DisplayFor(modelItem => item.PostName) @*/Modified: @Html.DisplayFor(modelItem => item.ModifyDate)*@

                                maxLeft--;
                            }
                            else
                            {
                                return;
                            }
                        }
                    </ul>
                </div>
                <div class="col ">
                    <h4 class="submenuhead ">Game Development</h4>
                    <ul class="list-unstyled ">
                        @foreach (var item in Model.Where(c => c.PostCategory == "Game Development"))
                        {
                            int maxLeft = 5;
                            if (maxLeft > 0)
                            {
                                <li> @Html.ActionLink(item.PostName, "Details", new { id = item.Id }, new { @class = "btn btn-info" })</li>
                                @*<a class="nav-link text-dark" asp-area="" asp-controller="Post" asp-action="Index"> @Html.DisplayFor(modelItem => item.PostName) @*/Modified: @Html.DisplayFor(modelItem => item.ModifyDate)*@

                                maxLeft--;
                            }
                            else
                            {
                                return;
                            }
                        }
                    </ul>
                </div>
                <div class="col ">
                    <h4 class="submenuhead ">Career/Learning</h4>
                    <ul class="list-unstyled ">
                        @foreach (var item in Model.Where(c => c.PostCategory == "Career/Learning"))
                        {
                            int maxLeft = 5;
                            if (maxLeft > 0)
                            {
                                <li> @Html.ActionLink(item.PostName, "Details", new { id = item.Id }, new { @class = "btn btn-info" })</li>
                                @*<a class="nav-link text-dark" asp-area="" asp-controller="Post" asp-action="Index"> @Html.DisplayFor(modelItem => item.PostName) @*/Modified: @Html.DisplayFor(modelItem => item.ModifyDate)*@

                                maxLeft--;
                            }
                            else
                            {
                                return;
                            }
                        }
                    </ul>
                </div>
                <div class="col-1 "></div>
            </div>
            
        </div>
    </div>
